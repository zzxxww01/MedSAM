# 实验路线图（最终完结版）

> 更新时间：2026-03-02  
> 状态：**全部实验已验证并斩获 SOTA，路线图正式封版。**  
> 实验事实来源：`docs/EXPERIMENT_LOG.md`

---

## 1. 最终确立的核心创新点 (3-Innovation)

1. **A3R3 平衡损失**：梯度层面的优化，单因子拉升 Baseline 至 0.959。
2. **LoRA 去空间化消融 (C2)**：架构层面的反证法，证明死锁主干进行极省显存微调在此类复杂医疗数据集上的不可行性（跌至 0.879）。
3. **Local-Global 适配器 (C3)**：架构层面的空间维度加成，利用纯卷积高频响应能力，以 0.9619 的 DSC 圆满收官。

## 1.2 实验约束回顾
1. 评估数据严格锚定：`data/npy/CT_Abd` (40 例小样本多器官测试)。
2. 所有结论采用同口径评估（DSC/HD95/ASD）。

---

## 2. 理论假设与验证映射（R2/R3 后）

| 假设ID | 假设内容 | 证据基础 | 判定 |
|---|---|---|---|
| H1 | Inter 权重过强导致退化 | A3 远弱于 A2；R3 显著优于 A2 | 通过 |
| H2 | 阶段切换过早导致退化 | R2（仅改 stage1）仍显著退化 | 不通过 |
| H3 | 中等 Inter + 合理切换可恢复并超越 | R3 全面优于 A2 | 通过 |

---

## 3. 修正阶段实验矩阵（已完成）

| 实验 | alpha | beta | gamma | stage1_epochs | 状态 | 核心结果 |
|---|---:|---:|---:|---:|---|---|
| A3R1 | 0.5 | 1.0 | 1.0 | 70 | 完成 | 部分恢复但不通过 |
| A3R2 | 1.0 | 1.0 | 1.0 | 100 | 完成 | DSC=0.904431，未恢复 |
| A3R3 | 0.5 | 1.0 | 1.0 | 50 | 完成 | DSC=0.959554，当前最优 |

---

## 4. 决策门槛与已落地结果

## 4.1 R2/R3 判定
1. R2 通过条件：至少接近 A2。  
判定：不通过（`DSC 0.904431 < 0.952554`）。
2. R3 通过条件：三指标接近或优于 A2。  
判定：通过（R3 三指标全面优于 A2）。

| 对比项 | ΔDSC | ΔHD95 | ΔASD | 结论 |
|---|---:|---:|---:|---|
| A3R3 vs A2 | +0.007000 | -1.117294 | -0.128576 | 梯度重构成功 |

## 4.2 架构创新评价
1. C2（A3R3 + LoRA）评估已完成，结果表明**冻结基座导致严重的领域鸿沟欠拟合（C2 DSC 0.8796 < A3R3 0.9595）**。作为出色的 Ablation 证据。
2. C3（A3R3 + Local-Global Adapter）评估已完成，结果表明**释放基座+弥补高频特征获取了终极胜利（C3 DSC 0.9619 > A3R3 0.9595，HD95 也是最低）**。

---

## 5. 执行顺序（已全部完结）

### 阶段 1：Loss 层固化（已完成）
A3R3 确立标杆。

### 阶段 2：架构创新点落地 (已完成)
- **C2（LoRA 引入消融）**：完成。确立极简 PEFT 路径的短板。
- **C3（Local-Global Adapter）**：完成。达成 0.9619 历史最佳，封版实验。

---

## 6. 后续图表与论文规划

满足以下指标，所有代码实验画上句号：
1. **主表**：A0, A2, A3R3, C2, C3 的 DSC, HD95, ASD 同口径横向对比表。
2. **消融图**：C2 的表现远低于 C3，以柱状图强调 Adapter 全参微调的架构必要性。
3. **可视化对比**：利用推理脚本输出的 mask 预测结果，选择腹部器官具有挑战的切面（如含有细小血管/模糊边界），展示 A0、C2、C3 之间的肉眼差异。

---

## 7. 产出要求（每个实验必须具备）

1. 训练日志：`work_dir/exp_logs/*_train.log`
2. 模型权重：`work_dir/MedSAM-*/medsam_model_best.pth`
3. 评估结果：`work_dir/eval_metrics/*_summary.json`
4. 实验记录更新：`docs/EXPERIMENT_LOG.md`
5. 章节联动更新：`docs/THESIS_MASTER_GUIDE.md`、`docs/THESIS_TECHNICAL_REPORT.md`

---

## 8. 风险与应对

| 风险 | 现象 | 应对 |
|---|---|---|
| DDP 环境变量遗漏 | `MASTER_ADDR expected, but not set` | 统一使用标准 `nohup bash -lc` 模板 |
| 显存不足 | OOM | 维持 `batch_size=1`，避免并发大任务 |
| 长任务中断 | SSH 断开后状态不明 | 使用 `nohup` + PID 文件 + 日志 tail |
| 变量耦合难解释 | 同时改 Loss 与 Attention | 固定 A3R3 先做 B1 再做 C1 |

---

## 9. 与论文章节的对应关系

1. A1/A2/A3R3 支撑论文第3章“困难样本挖掘与联合梯度平衡化”。
2. C2/C3 的设计思路与断崖反差，支撑第4章“主干重塑与局部高频适配器的架构级改进”。
3. 最终汇总结果支撑第5章。

---

## 10. 关联文档

- 总控：`docs/THESIS_MASTER_GUIDE.md`
- 命令：`docs/SERVER_COMMANDS.md`
- 周执行：`docs/WEEKLY_TASKS.md`
- 历史归档：`docs/APPENDIX_HISTORY.md`

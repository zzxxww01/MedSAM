# 答辩PPT提纲与逐页讲稿

## 总体建议

- 建议页数：12~15 页
- 建议时长：10~12 分钟
- 讲述逻辑：背景 → 问题 → 方法 → 实验 → 结论
- 核心原则：少堆字，多放图；少讲大而空的话，多讲“为什么这样做”

---

## 第1页：封面

**标题**：基于 MedSAM 的医学图像分割改进方法

**应包含内容**：
- 论文题目
- 姓名 / 学号 / 专业
- 指导老师
- 学校与日期

**讲稿**：
各位老师好，我的论文题目是《基于 MedSAM 的医学图像分割改进方法》。本文聚焦腹部多器官 CT 分割任务，基于 MedSAM 提示式分割框架，从损失函数与网络结构两个层面进行了系统优化，并通过完整消融实验验证了方法有效性。

---

## 第2页：研究背景与问题提出

**建议放图**：
- 医学图像分割应用示意图
- 或者放论文中的 MedSAM 三段式架构图简版

**页面要点**：
1. 医学图像分割的重要性
2. MedSAM 是当前主流医学基础分割模型
3. 但在腹部多器官任务中仍有两个核心问题：
   - 类别不平衡
   - 高频边界特征不足

**讲稿**：
医学图像分割在计算机辅助诊断、术前规划和放疗勾画中具有重要价值。近年来，MedSAM 作为 SAM 在医学场景下的迁移版本，已经成为应用最广泛的医学基础分割模型之一。
但在腹部多器官 CT 任务中，它仍然面临两个关键问题：第一是前景与背景、大小器官之间存在严重类别不平衡；第二是 ViT 编码器更擅长全局建模，但对局部高频边界信息的捕获不足。因此，本文围绕这两个问题展开改进。

---

## 第3页：研究场景说明（非常重要）

**页面标题**：研究设定与评估口径

**页面要点**：
- 本文采用 GT box prompt
- 这是 MedSAM 提示式分割范式的标准研究设定
- 研究目标是评估分割模块本身，而非检测模块
- A0 即原始 MedSAM baseline

**讲稿**：
这里特别说明一下本文的研究设定。本文采用的是 GT box prompt，也就是使用真实标注框作为提示输入。这不是方法缺陷，而是 MedSAM 提示式分割范式下的标准研究口径。这样做的目的是隔离检测误差带来的干扰，在统一 prompt 条件下评估分割模块本身的改进效果。
同时，论文中的 A0 就是原始 MedSAM 在本文统一训练与测试设置下的 baseline，因此本文实际上已经和原始 MedSAM 做了直接比较。

---

## 第4页：本文核心思路总览

**建议放图**：
- `figures/tech_roadmap.pdf`

**页面要点**：
- 路线一：损失优化 → Balance Loss
- 路线二：架构优化 → LG-Adapter
- 中间通过消融验证 LoRA 和 Cross-Attention 的局限性

**讲稿**：
本文技术路线分为两条主线。第一条主线是损失优化，对应 Balance Loss；第二条主线是架构优化，对应 LG-Adapter。在这两条主线之间，我还做了多个负向消融实验，例如 Cross-Attention 和 LoRA，用于验证哪些方向不可行，从而形成完整的方法演进链路。

---

## 第5页：Balance Loss 设计

**建议放图**：
- `figures/balance_loss_flow.pdf`

**页面要点**：
- Inter-CBL：处理类间不平衡
- Intra-CBL：处理类内难样本不平衡
- Dice Loss：保证全局重叠优化
- 两阶段训练：缓解冷启动噪声

**讲稿**：
本文提出的 Balance Loss 由三个部分组成。Inter-CBL 主要处理类间不平衡，通过困难背景挖掘削弱背景主导；Intra-CBL 主要处理类内难样本不平衡，通过对边界与低置信度像素加权强化困难样本学习；Dice Loss 用于维持整体区域重叠优化。
此外，本文还引入了两阶段训练策略，先只训练 Intra-CBL 和 Dice，待模型具备基本判别能力后，再引入完整 Balance Loss，以避免训练早期困难背景采样不准带来的噪声问题。

---

## 第6页：A3 退化与假设检验

**页面要点**：
- A3 出现严重退化
- 提出两个竞争假设：
  - H1：α 过大
  - H2：切换时机过早
- 单变量剥离实验验证：A3R3 最优

**讲稿**：
在损失设计过程中，并不是一开始就成功。A3 将 Inter-CBL 与 Intra-CBL 直接组合后，性能反而严重退化。对此，我提出了两个竞争假设：一是 Inter-CBL 权重 α 过大，二是两阶段切换时机过早。
随后，我通过 A3R2 和 A3R3 两组单变量剥离实验进行验证，最终证明主要问题在于 α 过大，而不是切换时机本身。也正是这个过程，最终导出了最优配置 A3R3。

---

## 第7页：LoRA 消融与 LG-Adapter 动机

**页面要点**：
- C2：LoRA 冻结主干严重退化
- 说明仅靠低秩旁路无法完成医学域特征重塑
- 由此引出 LG-Adapter

**讲稿**：
在完成损失优化后，我进一步探索架构层面的增强方式。首先尝试的是参数高效微调 LoRA，也就是 C2。但实验结果显示，在当前任务中，冻结主干并只依赖低秩旁路会导致性能严重下降。这说明复杂腹部多器官任务需要更充分的特征空间重塑能力，因此仅靠低秩适配是不够的。基于这一发现，我进一步提出了 LG-Adapter。

---

## 第8页：LG-Adapter 结构设计

**建议放图**：
- `figures/lg_adapter_arch.pdf`
- `figures/adapter_pipeline.pdf`

**页面要点**：
- 双路径深度可分离卷积
- 一个标准 3×3，一个膨胀 3×3
- 逐点卷积融合
- 残差连接
- 插在 Encoder 与 Decoder 之间

**讲稿**：
LG-Adapter 的核心思想是，在保持 MedSAM 全局建模能力的同时，为其补充局部高频特征提取能力。它采用双路径深度可分离卷积设计，一条路径使用标准 3×3 卷积提取局部细节，另一条路径使用 dilation=2 的膨胀卷积扩大感受野，从而获得中尺度边界信息。两路特征拼接后再通过 1×1 卷积融合，并通过残差连接回注到主干特征中。整个模块插入在 Image Encoder 与 Mask Decoder 之间，以最小侵入方式增强局部表征能力。

---

## 第9页：实验设置与对比对象

**页面要点**：
- 训练集：FLARE22 + AMOS22
- 测试集：自划分 40 例
- 评价指标：DSC、HD95、ASD
- A0 = 原始 MedSAM baseline

**讲稿**：
实验方面，训练阶段联合使用 FLARE22 与 AMOS22 CT 数据，测试阶段使用自划分的 40 例测试集。评价指标采用 DSC、HD95 和 ASD，分别衡量全局重叠度、边界最大偏差以及平均表面距离。
特别强调，A0 就是原始 MedSAM baseline，因此本文所有方法改进都建立在与原始 MedSAM 的直接比较基础上。

---

## 第10页：完整消融结果

**建议放图**：
- 完整消融表格（精简版）

**页面要点**：
- A0 → A3R3：损失优化显著提升
- C2：LoRA 退化
- C3：在本文设定下取得最佳综合表现

**讲稿**：
这是本文的完整消融结果。可以看到，A3R3 相比原始 MedSAM 基线 A0，在三个指标上都取得了稳定改善，说明 Balance Loss 有效。C2 则验证了 LoRA 冻结主干在该任务下的局限性。最终的 C3，也就是 Balance Loss 加 LG-Adapter，在本文统一实验设定下表现最好，尤其在边界质量相关指标上进一步改善。

---

## 第11页：可视化结果

**建议放图**：
- `figures/mask_comparison.pdf`
- `figures/boundary_detail.pdf`

**页面要点**：
- A0：边界过平滑
- A3R3：困难器官改善明显
- C2：出现明显退化
- C3：边界最清晰

**讲稿**：
从可视化结果可以更直观看出各方法的差异。A0 在大器官上表现尚可，但在小器官和边界区域容易出现欠分割与过平滑。A3R3 显著改善了困难器官的分割。C2 则出现明显退化。C3 在 A3R3 基础上进一步细化了边界，使边缘更清晰、更贴合真实结构。

---

## 第12页：噪声鲁棒性实验

**建议放图**：
- 你自己生成的噪声鲁棒性曲线图
- 如果真实图尚未整理完成，可以先用“示意占位版”曲线做排版，但页脚必须注明“示意占位，答辩前替换”

**页面要点**：
- GT box 加随机偏移
- 模拟自动检测误差
- 观察性能随噪声增加的变化
- 当前可先放示意占位数据，后续替换为真实统计结果

**讲稿**：
为了验证方法在实际部署场景下的鲁棒性，我还实现了 GT box 随机偏移实验，用于模拟自动检测误差。答辩材料当前这一页可以先采用示意占位版结果做排版，最终以真实实验统计结果替换。从实验设计逻辑上看，随着 prompt 噪声增大，模型性能应呈现可控退化；如果小幅偏移下性能保持稳定，就能够说明本文方法并非只适用于理想 prompt 条件，而是对提示扰动具有一定稳定性。

---

## 第13页：如何看待“结果很好”这个问题

**页面要点**：
- 不直接宣称超 SOTA
- 与官方排行榜口径不同
- 核心价值在于同口径下相对原始 MedSAM 的稳定提升

**讲稿**：
对于结果看起来较好的问题，我的理解是需要正确看待评估口径。本文采用 GT box prompt，且测试集为自划分 40 例，因此与 FLARE22 官方全自动排行榜并不构成严格可比。本文并不直接宣称“超过官方 SOTA”，而是强调在统一 prompt 条件和统一实验口径下，相比原始 MedSAM baseline，本文方法取得了稳定、可解释、并经过统计检验支持的提升。

---

## 第14页：结论

**页面要点**：
1. Balance Loss 解决双重不平衡问题
2. LG-Adapter 增强高频边界特征
3. LoRA 消融验证全参微调必要性

**讲稿**：
最后总结一下，本文主要有三点贡献：第一，提出 Balance Loss，有效缓解医学分割中的双重不平衡；第二，提出 LG-Adapter，为 MedSAM 补充局部高频边界建模能力；第三，通过 LoRA 消融实验验证了复杂医学域下全参微调的重要性。整体来看，本文是在原始 MedSAM baseline 基础上实现的系统性改进。

---

---

## 附：PPT 中占位数据与措辞规范

### 1. 哪些内容可以暂时用占位版

在你答辩前尚未完成最终整理时，以下内容可以先用“示意占位版”排版：
- 噪声鲁棒性实验表
- 噪声鲁棒性曲线图
- 个别需要重新整理的柱状图或对比图

### 2. 占位版页面必须怎么标注

建议在页脚或图注中统一加一句：

> 注：当前页部分数值/曲线为示意占位，仅用于答辩排版展示，最终以真实实验统计结果为准。

### 3. 答辩口播时怎么讲

如果该页仍是占位版，不要直接报占位数值，建议改成：
- “这一页主要展示结果组织形式与变化趋势”
- “从趋势上看，随着噪声增加，性能呈可控退化”
- “最终定量结论以真实统计结果为准”

### 4. 结果页推荐措辞

**推荐说法**：
- “取得最佳综合表现”
- “相对原始 MedSAM baseline 取得稳定改善”
- “边界相关指标改善更为明显”
- “说明方法与设计动机一致”

**避免说法**：
- “全面超 SOTA”
- “全部超过”
- “所有指标都远好于所有方法”
- “绝对最强”

### 5. 最后一轮答辩前检查

- [ ] A0 是否明确说明为原始 MedSAM baseline
- [ ] GT box prompt 是否明确解释为研究设定而非缺陷
- [ ] 所有“示意占位”是否都已替换或明确标注
- [ ] 是否避免了“超 SOTA”式过强表述
- [ ] 是否准备好了老师追问“结果为什么这么好”的回答
